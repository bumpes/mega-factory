# PRD：科幻巨构自动化工作流（巨构工厂）

版本：v1.0（第一版开发唯一依据）
日期：2026-09-25
状态：需求已确认

---

## 1. 产品目标和使用场景

### 1.1 产品目标
构建一个本地 Web 应用，实现"创意 → 提示词 → ComfyUI 批量生图 → 归档 → 与参考图比较打分"的科幻巨构图片自动化生产流水线，供个人使用。

### 1.2 使用场景
- 用户是 AI 视频/图像创作者（科幻巨构方向），硬件为 RTX 5090 Laptop 24GB。
- 本地已部署 ComfyUI（D:\ComfyUI_windows_portable），使用 qwen_image_edit_2511_bf16.safetensors 模型，API 端口 :8188。1K 分辨率单张约 30 秒。
- 用户希望批量生成科幻巨构概念图（调试期每创意 3 张，后续更多），按"输出/创意名/日期/"自动归档，每张图配套提示词（txt）、细节化生活化文案（md）。
- 用户会积累约 20 张他人高级巨构成品图作为"参考图库"，希望将自己的生成图与参考图并排比较、按统一维度打分、记录改进点。
- 访问方式：本机浏览器 + 同一局域网内手机浏览器（如 http://192.168.x.x:3000）。电脑关机/服务停止时手机不可访问，可接受。
- 无用户注册登录（仅个人使用），无社交功能。
- 数据全部本地持久化，下次进入依旧可以看到所有创意、作品、比较记录、设置。

### 1.3 关键外部依赖
| 依赖 | 用途 | 说明 |
|---|---|---|
| ComfyUI API (:8188) | 图片生成 | 已部署，直接调用 |
| 元宝深度思考 API | 创意生成、提示词生成、配套文案生成 | 云端 LLM；**必须通过统一的"云端文本 LLM 抽象层"接入**，可替换为通义千问等其他 API。API Key 在设置页配置。 |
| 本地文件系统 | 归档存储 | 输出目录、参考图库目录、创意库数据、配置文件 |

---

## 2. 整体页面框架和导航结构

### 2.1 页面清单
| 编号 | 页面 | 一句话职责 |
|---|---|---|
| P1 | 工作台（首页） | 系统状态、任务进度、最近产出、全局入口 |
| P2 | 创意库 | 浏览/搜索预置与 AI 生成创意，查看编辑文案与提示词，发起生成 |
| P3 | 生成任务 | 选创意、定参数、跑批、实时监控 |
| P4 | 作品库 | 按"创意名/日期"归档结构浏览全部生成图，灯箱查看，打包下载 |
| P5 | 比较工作台 | 生成图 vs 参考图并排 + 7 维度手动打分 + 改进点笔记 |
| P6 | 参考图库 | 上传管理约 20 张参考图，维护统一特征卡 |
| P7 | 设置 | ComfyUI 地址、元宝 API Key、路径、生成默认值、局域网访问信息 |

### 2.2 导航结构
- 桌面端：左侧固定侧边栏，7 个页面入口 + LOGO。
- 手机端：底部 Tab 栏 5 项——[工作台] [创意库] [生成] [作品库] [我的]；"我的"内收纳比较工作台、参考图库、设置。
- 响应式单页应用（SPA），同一套页面自适应桌面/手机。前端技术栈不限（开发者选定，无既有框架需复用）。

---

## 3. 首页（P1 工作台）的内容结构

自上而下：
1. **状态卡片行**（手机端纵向堆叠）：
   - ComfyUI 连接状态（绿=在线/红=离线，:8188 心跳检测，点击重试）
   - 创意库总数
   - 待比较作品数（已生成但未做比较的图数量）
2. **正在进行的任务**：任务名 + 实时进度条（n/m 张）+ 预计剩余时间 + [查看] [取消] 按钮；无任务时隐藏。
3. **最近生成**：横向滚动缩略图带，点击跳转作品详情（灯箱）。
4. **最近比较记录**：列表（创意名 | 总分 | 最弱维度 | 日期），点击跳转比较记录详情。
5. **全局最醒目按钮**：[发起新任务] → 直达 P3。

任务进度通过 WebSocket（或等价的实时推送机制）刷新，无需手动刷新页面。

---

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| P1 工作台 | 一眼看清系统状态与进行中任务，提供快速入口 |
| P2 创意库 | 巨构创意的"仓库+编辑器"。来源：① 系统预置的大型巨构主题库（戴森球、环形世界、太空电梯、恒星引擎、壳世界、轨道环等 + 风格/环境/尺度修饰词组合，量级 100+）② AI（元宝深度思考）按需生成新创意补充入库。每条创意含：名称、分类标签、科幻设定描述、**细节化生活化配套文案**、生成提示词。提示词默认内置风格模板：**强调巨构、压倒性史诗尺度**。 |
| P3 生成任务 | 批量生成执行中心。支持多选创意/随机抽 N 条，每条创意可设张数（默认 3）与分辨率（默认 1024×1024），提示词可逐条微调；串行调用 ComfyUI API 生成；实时回显每张进度/缩略图；完成自动归档；失败可重试单张；可取消未开始的排队图。可选"完成后自动进入比较工作台"。 |
| P4 作品库 | 磁盘归档结构的 Web 化浏览。左侧创意文件夹树（与"输出/创意名/日期/"一一对应），右侧日期分组图片网格；灯箱大图（翻页/下载/查看提示词/查看文案/去比较）；每张图带比较状态标记（未比较/已比较+总分）；多选打包下载 zip。 |
| P5 比较工作台 | 质检环节。我的生成图（A）vs 参考图（B）并排大图，支持双图同步缩放/平移；按 7 维度（尺度感、构图、光影、细节密度、材质、氛围、色彩）滑块打分 1-5，即时计算差值，差距 ≥2 自动高亮 ⚠ 并在笔记区生成引导模板；综合分对比；改进点笔记；保存后生成结构化比较报告（md + json）。参考图若已有特征卡则自动带入 B 侧评分。 |
| P6 参考图库 | 管理约 20 张高级成品参考图。上传/删除，文件名自动规范化；每张图维护**统一特征卡**（7 维度星级 + 文字亮点备注），第一版手动填写，页面预留"AI 自动提取特征"按钮位（灰色，标注 v2）。 |
| P7 设置 | ComfyUI 地址与连接测试；元宝 API Key 与测试、深度思考模式开关；输出目录/参考图库目录配置；生成默认值（张数、分辨率）；提示词风格模板编辑（可改）；局域网访问地址显示 + 复制 + 二维码（供手机扫码）。所有配置持久化到本地配置文件。 |

---

## 5. 每个模块第一版最必要的数据和操作

### P1 工作台
- 数据：ComfyUI 在线状态、创意总数、待比较数、当前任务进度、最近 10 张生成图、最近 10 条比较记录。
- 操作：刷新连接状态、跳转各页、取消当前任务。

### P2 创意库
- 数据（每条创意）：id、名称、分类标签、来源（预置/AI生成）、设定描述、配套文案（md）、提示词、创建时间、历史生成记录（日期+张数）。
- 操作：搜索、按来源/是否生成过筛选、AI 生成新创意（一次 5/10/20 条可选）、查看详情抽屉、编辑并保存文案与提示词、快捷发起生成、随机抽 N 条。

### P3 生成任务
- 数据（每个任务）：任务 id、选中创意列表、每创意张数、分辨率、各张图状态（排队/生成中/完成/失败）、耗时、归档路径、使用的提示词快照。
- 操作：多选创意、随机抽取、微调提示词、开始生成、实时查看进度、取消任务、失败重试单张、完成后跳转作品库/比较工作台。

### P4 作品库
- 数据：归档目录树（创意名/日期）、每张图的：图片文件、提示词 txt、文案 md、比较状态（无/已比较+总分）。
- 操作：树状浏览、灯箱查看（翻页/缩放）、下载单图、多选打包下载 zip、复制提示词/文案、跳转比较工作台。

### P5 比较工作台
- 数据（每条比较记录）：记录 id、A 图（生成图路径）、B 图（参考图路径）、7 维度双方评分、差值、综合分、改进点笔记文本、时间、关联创意名。
- 操作：选择 A 图（从作品库）、选择 B 图（随机🎲/指定）、同步缩放平移、滑块打分、填写笔记、保存记录（写入 输出/创意名/日期/比较报告.md + json）、查看历史记录列表。

### P6 参考图库
- 数据（每张参考图）：图片文件、规范化文件名、特征卡（7 维度星级、亮点备注、是否已填写）。
- 操作：上传（按钮/拖拽）、删除、查看大图、填写/保存特征卡。

### P7 设置
- 数据：ComfyUI 地址、元宝 API Key（加密或本地明文存储，仅本机）、深度思考模式开关、输出目录、参考图库目录、默认张数/分辨率、提示词风格模板文本。
- 操作：修改并保存各项、测试 ComfyUI 连接、测试元宝 API、复制局域网地址、显示二维码。

---

## 6. 首页、任务流程和各模块之间的关系

```
                ┌──────── P1 工作台 ────────┐
                │ 状态汇聚 + 全局入口 + 实时进度 │
                └──┬──────┬──────┬──────┬───┘
                   │      │      │      │
       发起新任务 ──┘      │      │      └── 查看比较记录
                   ▼      ▼      ▼              ▼
   AI生成/随机抽取 → P2 创意库 → P3 生成任务 → ComfyUI(:8188)
                    (创意+文案+提示词)   │
                                      ▼ 自动归档
                          输出/创意名/日期/(图+提示词txt+文案md)
                                      │
                                      ▼
                                 P4 作品库 ←──浏览/下载zip──┐
                                      │ 去比较              │
                                      ▼                    │
   P6 参考图库(特征卡) ──提供B侧──→ P5 比较工作台 ──保存──→ 比较报告md+json
                                      │                    │
                                      └── 记录回流到 P1/P4 状态标记
```

关系说明：
- **P1 是汇聚点**：不产生数据，聚合其他模块的状态（任务进度、待比较数、最近产出），是所有流程的起点。
- **P2 → P3 是生产入口**：创意（含文案、提示词）从创意库流入生成任务。
- **P3 → P4 是产出落盘**：生成完成即归档，作品库是磁盘目录的镜像。
- **P6 → P5 是质检基准**：参考图特征卡为比较提供 B 侧标准分。
- **P5 → P1/P4 是状态回流**：比较结果标记回作品库（已比较+总分）并出现在首页最近记录。
- 数据流单向闭环，所有节点本地持久化。

---

## 7. 第一版必须完成的功能（MVP 清单）

1. 预置大型巨构创意库（≥100 条，含分类标签）。
2. 元宝深度思考 API 生成新创意（一次 5/10/20 条），含设定描述、细节化生活化配套文案、提示词（内置"强调巨构、压倒性史诗尺度"风格模板）。
3. 云端文本 LLM 抽象层（元宝为默认实现，可配置替换为通义千问等）。
4. 批量生成：多选/随机抽创意，每创意默认 3 张、1024×1024，串行调用 ComfyUI qwen_image_edit 工作流，实时进度回显，失败重试单张，可取消排队。
5. 自动归档：`输出/创意名/日期/` 内保存 图片 + 提示词.txt + 文案.md。
6. 作品库：目录树浏览、灯箱大图、查看/复制提示词与文案、多选打包下载 zip、比较状态标记。
7. 参考图库：上传/管理约 20 张参考图，手动填写 7 维度特征卡（预留 v2 AI 提取按钮位）。
8. 比较工作台：A/B 并排 + 同步缩放平移、7 维度滑块打分、差值高亮（≥2 ⚠）、改进点笔记、保存比较报告（md + json）、历史记录回看。
9. 工作台首页：ComfyUI 状态心跳、任务实时进度、最近生成、最近比较、发起新任务入口。
10. 设置页：ComfyUI 地址测试、元宝 Key 测试、路径配置、生成默认值、风格模板编辑、局域网地址+二维码。
11. 响应式 UI：桌面（侧边栏）+ 手机（底部 Tab），手机可完成全部操作（含发起任务）。
12. 全量本地持久化：重启服务后创意、作品、比较记录、设置、特征卡全部保留。

---

## 8. 第一版暂时不做的功能（明确排除）

1. AI 视觉模型自动比较分析（v2；第一版仅并排展示 + 手动打分）。
2. 参考图特征卡 AI 自动提取（v2；第一版手动填写，仅预留按钮位）。
3. "根据改进建议一键修改提示词并重新生成"的闭环（仅记录展示改进点）。
4. 用户注册/登录系统。
5. 社交/分享功能。
6. 云端部署/公网访问（仅本机 + 局域网）。
7. 生成队列并行化（第一版串行，受 24GB 显存限制）。

---

## 9. 验收标准（可实际检查）

### A. 创意库
- [ ] A1 首次启动后创意库列表 ≥100 条预置创意，每条含名称、标签、设定描述、文案、提示词。
- [ ] A2 点击"AI 生成新创意"选择 10 条，成功后列表顶部出现 10 条带 NEW 角标的新创意；断网或 Key 错误时给出明确错误提示且不崩溃。
- [ ] A3 编辑某条创意的文案并保存，刷新页面/重启服务后修改仍在；对应 md 文件内容同步更新。
- [ ] A4 每条 AI 创意的提示词中包含巨构/史诗尺度相关风格描述（风格模板生效）。

### B. 生成任务
- [ ] B1 选择 2 条创意、每条 3 张，点击开始，页面显示预计耗时（约 180s）；6 张图全部完成且实时逐张回显缩略图。
- [ ] B2 生成期间 ComfyUI 被关闭，未完成的图标记失败并显示错误，[重试单张] 在 ComfyUI 恢复后可成功。
- [ ] B3 任务进行中点击取消，排队中的图不再执行，已完成的图保留。
- [ ] B4 任务完成后检查磁盘：存在 `输出/<创意名>/<日期>/` 目录，内含 3 张图片 + 提示词.txt + 文案.md，内容对应该创意。

### C. 作品库
- [ ] C1 作品库目录树与磁盘 `输出/` 目录结构一致；新归档的批次无需刷新页面延迟即可见（或手动刷新后可见）。
- [ ] C2 灯箱中可翻页、查看提示词与文案并复制成功。
- [ ] C3 多选 4 张图打包下载，得到 zip，解压后文件完整。
- [ ] C4 已做过比较的图显示"已比较 + 总分"标记，未比较的显示"未比较"。

### D. 参考图库
- [ ] D1 上传 2 张图后图库计数 +2，文件出现在参考图库目录且文件名已规范化。
- [ ] D2 为某参考图填写 7 维度特征卡并保存，重启后仍在；在比较工作台选择该参考图时 B 侧评分自动带入。
- [ ] D3 页面存在灰色的"AI 自动提取特征（v2）"按钮位，点击不报错（提示 v2 提供）。

### E. 比较工作台
- [ ] E1 选择一张生成图和一张参考图，双图并排显示；在桌面端缩放/平移 A 图时 B 图同步。
- [ ] E2 7 个维度滑块打分，差值实时计算；差距 ≥2 的维度出现 ⚠ 高亮且笔记区出现引导模板。
- [ ] E3 保存记录后：`输出/<创意名>/<日期>/` 下生成 比较报告.md 与 json；P5 历史列表出现该记录；P4 中对应图片状态变为"已比较+总分"；P1 最近比较记录出现该条。
- [ ] E4 手机端（局域网访问）可完成 A/B 切换查看与打分保存全流程。

### F. 全局与持久化
- [ ] F1 桌面端左侧栏 7 项齐全；手机端底部 5 Tab 齐全，"我的"内可进入比较工作台/参考图库/设置。
- [ ] F2 手机浏览器通过 `http://<本机局域网IP>:3000`（以实际端口为准）可打开应用并完成"选创意→生成→查看→比较"全流程。
- [ ] F3 设置页显示局域网地址与二维码，扫码可直达。
- [ ] F4 重启服务后：创意库、作品、比较记录、参考图特征卡、全部设置项完整保留。
- [ ] F5 设置页 ComfyUI [测试连接] 在 :8188 运行时显示成功、关闭时显示失败；元宝 API [测试] 同理。
